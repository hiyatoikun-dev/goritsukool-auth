{
  "_plan": "Plan B確定 - Make関数のみ（外部API不要）",
  "_note": "1.text はMake画面上の実フィールド名に差し替えること",

  "sheets_column_mapping": {
    "C_処理区分":  "{{if(contains(1.text; \"入金\"); \"入金\"; if(contains(1.text; \"出金\"); \"出金\"; if(contains(1.text; \"振替\"); \"振替\"; if(contains(1.text; \"請求\"); \"請求\"; \"その他\"))))}}",
    "D_金額":      "{{ifempty(replace(toString(first(match(1.text; \"[0-9,]+\"))); \",\"; \"\"); \"\")}}",
    "E_予定日":    "{{ifempty(first(match(1.text; \"[0-9]{4}[-/][0-9]{1,2}[-/][0-9]{1,2}\")); \"\")}}",
    "F_タスク期限":"{{ifempty(first(match(1.text; \"[0-9]{4}[-/][0-9]{1,2}[-/][0-9]{1,2}\")); \"\")}}",
    "G_状態":      "未処理",
    "H_処理メモ":  "{{ifempty(left(1.text; 100); \"\")}}"
  },

  "limitations": {
    "予定日とタスク期限": "文脈区別不可のため同じ日付が入る",
    "金額": "文中の最初の数字列を取得。別数字が先にある場合は誤取得の可能性あり"
  }
}
